# csv-project fixture

A minimal project seed for the `spec` skill eval. The agent writes a spec for a
CSV-to-JSON CLI tool based on the task prompt.

Case that uses it:
- `spec-001` — write a PRD for a CSV→JSON CLI (large files, type inference, streaming)
