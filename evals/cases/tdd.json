[
  {
    "id": "tdd-001",
    "skill_under_test": "skills/06-test/tdd",
    "task_prompt": "Implement paginate(items, page, perPage) in src/paginate.js. It returns { items, totalItems, currentPage, totalPages, hasNext, hasPrev }. page is 1-indexed. Edge cases to handle: empty array returns empty items with totalPages=0; page beyond range returns empty items and hasNext=false; page=0 is treated as page 1 (some callers pass 0); perPage must be >= 1 (throw RangeError if 0 or negative). Use TDD.",
    "negative_control": false,
    "fixture": "evals/fixtures/tdd-starter",
    "grader": {
      "type": "hybrid",
      "code_check": {
        "command": "npm install --silent && npm test",
        "cwd": "{workspace}",
        "pass_condition": "exit_code == 0"
      },
      "llm_judge": {
        "agent": "evals/agents/grader.md",
        "expectations": [
          "A test file exists and was written before the implementation file (transcript evidence of ordering)",
          "The agent explicitly ran the test and observed it fail before writing implementation (RED phase)",
          "Tests cover the edge cases: empty array, page beyond range, page=0 treated as page 1, perPage=0 throws RangeError",
          "paginate([1,2,3,4,5], 2, 2) returns items [3,4] with totalPages=3 and hasNext=true",
          "paginate([1,2,3,4,5], 0, 2) returns items [1,2] (page=0 treated as page 1), not empty",
          "paginate([1,2,3,4,5], 1, 0) throws RangeError",
          "paginate([], 1, 2) returns items [] with totalPages=0",
          "The full test suite passes with no skipped tests"
        ]
      }
    },
    "runs": 1,
    "timeout_seconds": 600,
    "notes": "Harder than parseDuration: page=0 negative-slice bug (slice(-perPage) returns []) is a real JS gotcha impl-first ships, test-first catches. Timeout 600s — with-skill prompt includes full SKILL.md."
  },
  {
    "id": "tdd-002",
    "skill_under_test": "skills/06-test/tdd",
    "task_prompt": "There's a bug in src/paginate.js: paginate([1,2,3,4,5], 1, 2) returns items [3,4] instead of [1,2]. The slice math is off by one page. Fix it using the Prove-It pattern — write a failing reproduction test first, watch it fail, then fix the root cause.",
    "negative_control": false,
    "fixture": "evals/fixtures/paginate-buggy",
    "grader": {
      "type": "hybrid",
      "code_check": {
        "command": "npm install --silent && npm test",
        "cwd": "{workspace}",
        "pass_condition": "exit_code == 0"
      },
      "llm_judge": {
        "agent": "evals/agents/grader.md",
        "expectations": [
          "The agent wrote a failing reproduction test BEFORE fixing the code",
          "The agent observed the test fail (confirmed the bug reproduces)",
          "The fix addresses the root cause (off-by-one in the slice start index), not a symptom",
          "The agent ran the full suite after the fix and it passes",
          "No debug instrumentation or TODO markers left behind"
        ]
      }
    },
    "runs": 1,
    "timeout_seconds": 600,
    "notes": "Prove-It pattern — write the failing test first, then fix. Buggy fixture pre-seeded."
  },
  {
    "id": "tdd-neg-001",
    "skill_under_test": "skills/06-test/tdd",
    "task_prompt": "Add a README.md with setup instructions for this Node.js project. Just a basic readme.",
    "negative_control": true,
    "fixture": "evals/fixtures/tdd-starter",
    "grader": {
      "type": "llm-judge",
      "agent": "evals/agents/grader.md",
      "expectations": [
        "The agent did NOT write any test files",
        "The agent did NOT mention TDD, red-green-refactor, or testing-first",
        "The agent produced a README.md"
      ]
    },
    "runs": 1,
    "timeout_seconds": 180,
    "notes": "Precision check: tdd should NOT activate for a documentation task"
  },
  {
    "id": "tdd-neg-002",
    "skill_under_test": "skills/06-test/tdd",
    "task_prompt": "Rename the function paginate to paginateItems across the codebase.",
    "negative_control": true,
    "fixture": "evals/fixtures/paginate-buggy",
    "grader": {
      "type": "llm-judge",
      "agent": "evals/agents/grader.md",
      "expectations": [
        "The agent did NOT invoke the TDD red-green-refactor workflow",
        "The agent did NOT write new tests for a mechanical rename",
        "The rename was completed across the file"
      ]
    },
    "runs": 1,
    "timeout_seconds": 180,
    "notes": "tdd explicitly says Not for mechanical renames"
  }
]
